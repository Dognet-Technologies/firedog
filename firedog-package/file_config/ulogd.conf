# Configurazione ulogd2 per firewall logging
# File: /etc/ulogd.conf

[global]
######################################################################
# GLOBAL OPTIONS
######################################################################

logfile="/var/log/ulogd/ulogd.log"
loglevel=5

# Plugin stack per PCAP
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_inppkt_NFLOG.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_filter_IFINDEX.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_filter_IP2STR.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_filter_PRINTPKT.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_output_PCAP.so"
plugin="/usr/lib/x86_64-linux-gnu/ulogd/ulogd_raw2packet_BASE.so"

######################################################################
# STACK: INPUT DROP (NFLOG group 1) + OUTPUT DROP (NFLOG group 2)
# NB: tutte le righe stack= devono stare in [global] — se finiscono in
# un'altra sezione ulogd >= 2.0.9 (openSUSE) rifiuta la configurazione.
######################################################################

stack=log_input:NFLOG,base1:BASE,ifi1:IFINDEX,ip2str1:IP2STR,print1:PRINTPKT,pcap_input:PCAP
stack=log_output:NFLOG,base2:BASE,ifi2:IFINDEX,ip2str2:IP2STR,print2:PRINTPKT,pcap_output:PCAP

[log_input]
group=1
bind=1
# Buffer nflog per gestire burst di pacchetti. NB: nlbufsiz/snaplen sono
# chiavi del vecchio plugin ULOG: il plugin NFLOG usa bufsize e
# netlink_socket_buffer_size (ulogd >= 2.0.9 rifiuta le chiavi sconosciute).
# Il copy-range NFLOG è full-packet di default (niente snaplen).
bufsize=1048576
netlink_socket_buffer_size=1048576
netlink_socket_buffer_maxsize=2097152

[base1]

[ifi1]

[ip2str1]

[print1]

[pcap_input]
# File di output per traffico INPUT bloccato
file="/var/log/ulogd/input_dropped.pcap"
# Sync ogni 128 pacchetti per performance
sync=128

[log_output]
group=2
bind=1
bufsize=1048576
netlink_socket_buffer_size=1048576
netlink_socket_buffer_maxsize=2097152

[base2]

[ifi2]

[ip2str2]

[print2]

[pcap_output]
# File di output per traffico OUTPUT bloccato
file="/var/log/ulogd/output_dropped.pcap"
sync=128
