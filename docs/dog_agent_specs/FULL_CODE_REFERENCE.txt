================================================================================
FIREDOG DOG AGENT - CODICE COMPLETO PER IMPLEMENTAZIONE
================================================================================

NOTA: Questo file contiene TUTTO il codice fornito nella conversazione originale.
Usalo come riferimento completo per l'implementazione.

La documentazione nella cartella principale contiene le specifiche architetturali,
questo file contiene il codice completo pronto per essere copiato.

================================================================================
SEZIONE 1: BACKEND DJANGO - MODELS
================================================================================

File: firedog_backend/agent_manager/models.py

[VEDERE MESSAGGIO ORIGINALE SEZIONE 3.2 - Models Completi]

Contiene:
- AgentAPIKey (con hash_key, verify_key methods)
- PairingSession (con verify_phase_1, verify_phase_2)
- AgentConnection (con update_heartbeat, mark_offline)
- AgentCommand (con mark_sent, mark_executing, mark_success, mark_failed)
- AgentHeartbeat

================================================================================
SEZIONE 2: BACKEND DJANGO - SERIALIZERS
================================================================================

File: firedog_backend/agent_manager/serializers.py

[VEDERE MESSAGGIO ORIGINALE SEZIONE 3.3 - Serializers]

Contiene tutti i serializers DRF per i 5 models.

================================================================================
SEZIONE 3: BACKEND DJANGO - VIEWS (REST API)
================================================================================

File: firedog_backend/agent_manager/views.py

[VEDERE MESSAGGIO ORIGINALE SEZIONE 3.4 - Views]

Contiene:
- AgentAPIKeyViewSet (con generate, deactivate, activate actions)
- PairingSessionViewSet (con start, status actions)
- AgentConnectionViewSet (con online, offline, disconnect actions)
- AgentCommandViewSet (con pending, retry actions)

================================================================================
SEZIONE 4: BACKEND DJANGO - WEBSOCKET CONSUMER
================================================================================

File: firedog_backend/agent_manager/consumers.py

[VEDERE MESSAGGIO ORIGINALE SEZIONE 3.5 - WebSocket Consumer]

Contiene AgentConsumer completo con:
- handle_pair_request
- handle_heartbeat
- handle_threat_log
- handle_command_response
E tutti i metodi database @database_sync_to_async

================================================================================
SEZIONE 5: BACKEND DJANGO - WEBSOCKET ROUTING
================================================================================

File: firedog_backend/agent_manager/routing.py
File: firedog_backend/firedog_backend/asgi.py

[VEDERE MESSAGGIO ORIGINALE SEZIONE 3.6]

================================================================================
SEZIONE 6: BACKEND DJANGO - CELERY TASKS
================================================================================

File: firedog_backend/agent_manager/tasks.py
File: firedog_backend/firedog_backend/celery.py (update)

[VEDERE MESSAGGIO ORIGINALE SEZIONE 3.7]

Contiene:
- check_agent_health
- cleanup_old_heartbeats
- cleanup_expired_pairing_sessions
- timeout_stale_commands
- check_critical_threats

================================================================================
SEZIONE 7: AGENT PYTHON - MAIN
================================================================================

File: /opt/sentinelsuite/firedog/dog_agent.py

[VEDERE MESSAGGIO ORIGINALE SEZIONE 5.3 - Main Agent Script]

Classe DogAgent completa con:
- start/stop lifecycle
- send_pairing_request
- heartbeat_loop
- threat_analysis_loop
- integrity_check_loop
- handle_command
- handle_config_update

================================================================================
SEZIONE 8: AGENT PYTHON - CONFIG MANAGER
================================================================================

File: /opt/sentinelsuite/firedog/config_manager.py

[VEDERE MESSAGGIO ORIGINALE SEZIONE 5.4]

================================================================================
SEZIONE 9: AGENT PYTHON - WEBSOCKET CLIENT
================================================================================

File: /opt/sentinelsuite/firedog/websocket_client.py

[VEDERE MESSAGGIO ORIGINALE SEZIONE 5.5]

NOTA: Il codice continua ma è stato troncato nel messaggio originale.
CODICE COMPLETO da completare:


class WebSocketClient:
    def __init__(self, server_url, api_key, verify_ssl=True):
        self.server_url = server_url.replace('https://', 'wss://').replace('http://', 'ws://')
        self.ws_url = f"{self.server_url}/ws/agent/"
        self.api_key = api_key
        self.verify_ssl = verify_ssl
        self.websocket = None
        self.connected = False
        self.handlers = {}
        self.receive_task = None
    
    async def connect(self):
        ssl_context = None if not self.verify_ssl else True
        self.websocket = await websockets.connect(self.ws_url, ssl=ssl_context)
        self.connected = True
        self.receive_task = asyncio.create_task(self.receive_loop())
        return True
    
    async def disconnect(self):
        self.connected = False
        if self.receive_task:
            self.receive_task.cancel()
        if self.websocket:
            await self.websocket.close()
    
    async def send(self, data: dict):
        if not self.connected or not self.websocket:
            return False
        message = json.dumps(data)
        await self.websocket.send(message)
        return True
    
    async def receive_loop(self):
        while self.connected:
            try:
                message = await self.websocket.recv()
                data = json.loads(message)
                message_type = data.get('type')
                if message_type in self.handlers:
                    await self.handlers[message_type](data)
            except websockets.exceptions.ConnectionClosed:
                self.connected = False
                break
            except Exception as e:
                logger.error(f"Error in receive loop: {e}")
    
    def on_message(self, message_type: str, handler):
        self.handlers[message_type] = handler

================================================================================
SEZIONE 10: AGENT PYTHON - THREAT DETECTOR
================================================================================

[VEDERE MESSAGGIO ORIGINALE SEZIONE 5.6 - Threat Detector]

================================================================================
SEZIONE 11: AGENT PYTHON - FIREWALL MANAGER
================================================================================

[VEDERE MESSAGGIO ORIGINALE SEZIONE 5.7 - Firewall Manager]

================================================================================
SEZIONE 12: AGENT PYTHON - SYSTEM MONITOR
================================================================================

[VEDERE MESSAGGIO ORIGINALE SEZIONE 5.8 - System Monitor]

================================================================================
SEZIONE 13: AGENT PYTHON - INTEGRITY MONITOR
================================================================================

[VEDERE MESSAGGIO ORIGINALE SEZIONE 5.9 - Integrity Monitor]

================================================================================
SEZIONE 14: DEBIAN PACKAGE - CONTROL FILE
================================================================================

[VEDERE MESSAGGIO ORIGINALE SEZIONE 6.2 - Control File]

================================================================================
SEZIONE 15: DEBIAN PACKAGE - POSTINST SCRIPT
================================================================================

[VEDERE MESSAGGIO ORIGINALE SEZIONE 6.3 - postinst Script]

================================================================================
SEZIONE 16: DEBIAN PACKAGE - SYSTEMD SERVICE
================================================================================

[VEDERE MESSAGGIO ORIGINALE SEZIONE 6.6 - systemd Service File]

================================================================================
SEZIONE 17: CONFIGURATION FILES
================================================================================

agent.conf (JSON):
{
  "server": {
    "url": "https://firedog.example.com",
    "api_key": "generated-api-key-here",
    "verify_ssl": true,
    "reconnect_interval": 30
  },
  "agent": {
    "group": "production-servers",
    "notification_interval": 30,
    "log_level": "INFO",
    "log_path": "/opt/sentinelsuite/firedog/log"
  },
  "intervention": {
    "mode": "automatic",
    "block_type": "temporary",
    "block_duration": 3600,
    "threat_threshold": 75
  },
  "monitoring": {
    "check_integrity": true,
    "integrity_files": [
      "/opt/sentinelsuite/firedog/dog_agent.py",
      "/etc/dog-agent/agent.conf"
    ],
    "pcap_path_input": "/var/log/ulogd/input_dropped.pcap",
    "pcap_path_output": "/var/log/ulogd/output_dropped.pcap"
  }
}

requirements.txt (Agent):
websockets==12.0
psutil==5.9.6
scapy==2.5.0

================================================================================
FINE CODICE COMPLETO
================================================================================

ISTRUZIONI PER L'USO:

1. Copia ogni sezione nel file appropriato
2. Segui l'ordine di implementazione nel README.md
3. Testa ogni componente dopo l'implementazione
4. Riferirsi alla documentazione per dettagli architetturali

CONTATTI: Simone - FireDog Team

